'use client';

export const AddQuestion = () => {

    const addQuestion = () => {
        
    }

    return (
        <div>
            <button onClick={addQuestion} className="px-4 py-2">Add Question</button>
        </div>
    )
}